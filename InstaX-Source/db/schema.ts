import { sqliteTable, text, integer, primaryKey, index } from 'drizzle-orm/sqlite-core';
export const profiles = sqliteTable('profiles', { id: text('id').primaryKey(), name: text('name').notNull(), handle: text('handle').notNull().unique(), bio: text('bio').notNull().default(''), avatar: text('avatar').notNull().default(''), sample: integer('sample').notNull().default(0) });
export const posts = sqliteTable('posts', { id: text('id').primaryKey(), author: text('author').notNull().references(() => profiles.id), body: text('body').notNull(), image: text('image').notNull().default(''), created: integer('created').notNull(), sample: integer('sample').notNull().default(0) }, t => [index('idx_posts_created').on(t.created), index('idx_posts_author_created').on(t.author, t.created)]);
export const likes = sqliteTable('likes', { user: text('user').notNull().references(() => profiles.id), post: text('post').notNull().references(() => posts.id, { onDelete: 'cascade' }) }, t => [primaryKey({ columns: [t.user, t.post] }), index('idx_likes_post').on(t.post)]);
export const comments = sqliteTable('comments', { id: text('id').primaryKey(), user: text('user').notNull().references(() => profiles.id), post: text('post').notNull().references(() => posts.id, { onDelete: 'cascade' }), body: text('body').notNull(), created: integer('created').notNull() }, t => [index('idx_comments_post_created').on(t.post, t.created)]);
export const follows = sqliteTable('follows', { user: text('user').notNull().references(() => profiles.id), target: text('target').notNull().references(() => profiles.id) }, t => [primaryKey({ columns: [t.user, t.target] }), index('idx_follows_target').on(t.target)]);
export const bookmarks = sqliteTable('bookmarks', { user: text('user').notNull().references(() => profiles.id), post: text('post').notNull().references(() => posts.id, { onDelete: 'cascade' }), created: integer('created').notNull() }, t => [primaryKey({ columns: [t.user, t.post] }), index('idx_bookmarks_post').on(t.post)]);
export const notifications = sqliteTable('notifications', { id: text('id').primaryKey(), recipient: text('recipient').notNull().references(() => profiles.id), actor: text('actor').notNull().references(() => profiles.id), type: text('type').notNull(), post: text('post').references(() => posts.id, { onDelete: 'cascade' }), created: integer('created').notNull(), read: integer('read').notNull().default(0) }, t => [index('idx_notifications_recipient_created').on(t.recipient, t.created), index('idx_notifications_recipient_read').on(t.recipient, t.read)]);
